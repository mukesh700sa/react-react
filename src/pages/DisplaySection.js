import React from 'react'

const DisplaySection = () => {

  const per = {
    id: 1,
    name: rita
  };

  const { id, name } = per;

  return (

    <div>
      <button onClick={greet}




    </div>
  )
}

export default DisplaySection
